# Tessl Release Manager evals

These scenarios test the meta-skill's core judgment calls:
- when a changed skill needs a version bump and release tags
- when publish state is already healthy and should not be misdiagnosed as moderation failure
- when a new eval scenario is warranted because an important failure mode is currently uncovered
